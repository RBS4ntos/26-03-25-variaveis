//Subtraindo numeros

let a = 10; //define variavel temporaria com valor de 10
let b = 5; //define variavel temporaria com valor de 5

console.log(a - b); // mensagem no console com a subtração das variaveis a - b
console.log(b - a); //mensagem no console com a subtração das variaveis b - a